<UserControl x:Class="WinAssistant.Controls.AppPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox"
                 PlaceholderText="搜索应用程序..."
                 Margin="0,0,0,8" />

        <ListView x:Name="AppListView"
                  Grid.Row="1"
                  SelectionMode="None"
                  MinHeight="300" MaxHeight="420"
                  ItemTemplate="{StaticResource AppPickerTemplate}" />

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="RefreshButton" Grid.Column="1"
                    Content="刷新列表"
                    MinWidth="80" MinHeight="36"
                    Padding="16,6" CornerRadius="6" FontSize="14" />
            <Button Grid.Column="3" x:Name="CloseButton"
                    Content="关闭"
                    MinWidth="80" MinHeight="36"
                    Padding="16,6" CornerRadius="6" FontSize="14" />
        </Grid>
    </Grid>
</UserControl>
